paths:
  /api/auth/login:
    post:
      summary: Creates a user.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tel:
                  type: string
                password:
                  type: string
      responses: 
        '201':
          description: Created
